% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Binomial_fSuSiE.R
\name{vga_binomial_solver}
\alias{vga_binomial_solver}
\title{Solve the binomial logistic-normal VGA problem}
\usage{
vga_binomial_solver(
  init_val,
  x,
  trials,
  beta,
  sigma2,
  quadrature_order = 20L,
  maxiter = 300L,
  tol = 1e-07
)
}
\arguments{
\item{init_val}{Initial posterior-mean vector.}

\item{x}{Integer success-count vector.}

\item{trials}{Integer trial-total vector.}

\item{beta}{Gaussian prior mean, scalar or vector.}

\item{sigma2}{Positive Gaussian prior variance, scalar or vector.}

\item{quadrature_order}{Number of Gauss--Hermite nodes.}

\item{maxiter}{Maximum L-BFGS-B iterations.}

\item{tol}{Projected-gradient tolerance.}
}
\value{
A list containing posterior mean \code{m}, posterior variance \code{v}, the
  optimizer convergence code, and the optimized local variational objective.
}
\description{
Optimizes Gaussian variational posterior means and variances for independent
binomial observations with Gaussian prior means and variances. Stable
Gauss--Hermite quadrature is used for logistic-normal expectations.
}
\examples{
successes <- c(0, 3, 8, 10)
totals <- rep(10, length(successes))
vga_binomial_solver(
  init_val = stats::qlogis((successes + 0.5) / (totals + 1)),
  x = successes,
  trials = totals,
  beta = 0,
  sigma2 = 0.5
)
}
