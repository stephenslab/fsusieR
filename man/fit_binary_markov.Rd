% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm_routines.R
\name{fit_binary_markov}
\alias{fit_binary_markov}
\title{Fit the exact two-state binary Markov model}
\usage{
fit_binary_markov(
  y,
  se,
  state_means = c(0, 1),
  sequence_id = rep(1L, length(y)),
  q_interval = c(0, 0.5),
  grid_size = 501L,
  init_prob = c(0.5, 0.5)
)
}
\arguments{
\item{y}{Numeric vector of noisy observations.}

\item{se}{Numeric vector of known, strictly positive standard errors.}

\item{state_means}{Two finite point-state means.}

\item{sequence_id}{Vector identifying independent contiguous sequences.}

\item{q_interval}{Increasing length-two search interval contained in `[0,1]`.}

\item{grid_size}{Number of initial likelihood evaluations over `q_interval`.}

\item{init_prob}{Nonnegative length-two initial state distribution.}
}
\value{
A list containing the estimated flip probability `q`, transition
  matrix, log likelihood, smoothed state probabilities, and boundary
  probabilities.
}
\description{
Fits the point-state special case with a symmetric transition matrix
`A(q) = matrix(c(1-q, q, q, 1-q), 2, 2, byrow = TRUE)`. The scalar `q` is
selected by likelihood maximization over the requested interval.
}
